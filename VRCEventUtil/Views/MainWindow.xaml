<Window
    x:Class="VRCEventUtil.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
    xmlns:v="clr-namespace:VRCEventUtil.Views"
    xmlns:vm="clr-namespace:VRCEventUtil.ViewModels"
    xmlns:m="clr-namespace:VRCEventUtil.Models"
    xmlns:ctrls="clr-namespace:VRCEventUtil.Controls"
    xmlns:userlist="clr-namespace:VRCEventUtil.Models.UserList"
    Title="VRChat Event Util"
    Width="700" MinWidth="500"
    Height="750" MinHeight="650"
    WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <behaviors:Interaction.Triggers>
        <!--  When ContentRendered event raised, Initialize method of ViewModel would be called.  -->
        <behaviors:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodName="Initialize" MethodTarget="{Binding}" />
        </behaviors:EventTrigger>

        <!--  Dispose method is called, when Window closing.  -->
        <behaviors:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction />
        </behaviors:EventTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="InformationMessage">
            <l:InformationDialogInteractionMessageAction InvokeActionOnlyWhenWindowIsActive="False"/>
        </l:InteractionMessageTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="OpenFileDialog">
            <l:OpenFileDialogInteractionMessageAction InvokeActionOnlyWhenWindowIsActive="False"/>
        </l:InteractionMessageTrigger>

        <!--  If you make user choose 'OK or Cancel' closing Window, then please use Window Close cancel Behavior.  -->

    </behaviors:Interaction.Triggers>

    <Window.Resources>
    </Window.Resources>

    <Grid IsEnabled="{Binding IsLoading, Converter={StaticResource BoolInvertConverter}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="260"/>
            <RowDefinition Height="180"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>

        <!--メニューバー-->
        <Menu Grid.Row="0"/>

        <!--認証グリッド-->
        <GroupBox Grid.Row="1" Header="認証" Margin="5 5 5 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="16"/>
                    <RowDefinition Height="25"/>
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <Grid.Resources>
                    <Style x:Key="LoggedOutOnly" TargetType="Control">
                        <Setter Property="IsEnabled" Value="{Binding IsLoggedIn, Converter={StaticResource BoolInvertConverter}}"/>
                    </Style>
                </Grid.Resources>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ユーザー名"/>
                <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding Username}" ToolTip="Username" VerticalContentAlignment="Center" Style="{StaticResource LoggedOutOnly}"/>

                <TextBlock Grid.Row="0" Grid.Column="1" Text="パスワード"/>
                <PasswordBox Grid.Row="1" Grid.Column="1" ToolTip="Password" VerticalContentAlignment="Center" Style="{StaticResource LoggedOutOnly}">
                    <behaviors:Interaction.Behaviors>
                        <l:PasswordBoxBindingSupportBehavior Password="{Binding Password}"/>
                    </behaviors:Interaction.Behaviors>
                </PasswordBox>

                <TextBlock Grid.Row="0" Grid.Column="2" Text="二要素認証コード"/>
                <PasswordBox Grid.Row="1" Grid.Column="2" ToolTip="二要素認証コード" VerticalContentAlignment="Center" Style="{StaticResource LoggedOutOnly}">
                    <behaviors:Interaction.Behaviors>
                        <l:PasswordBoxBindingSupportBehavior Password="{Binding MFACode}"/>
                    </behaviors:Interaction.Behaviors>
                </PasswordBox>

                <Button Grid.Row="1" Grid.Column="3">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Content" Value="ログイン"/>
                            <Setter Property="Command" Value="{Binding LoginCommand}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                                    <Setter Property="Content" Value="ログアウト"/>
                                    <Setter Property="Command" Value="{Binding LogoutCommand}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </GroupBox>

        <TabControl Grid.Row="2" Margin="5" IsEnabled="{Binding IsLoggedIn}">
            <!--<TabItem Header="インスタンス管理">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="20"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" DataContext="{Binding WorldInstanceManager, Mode=OneWay}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="150"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding WorldId}" ToolTip="World ID" VerticalContentAlignment="Center"/>
                        <Button Grid.Column="1" Content="インスタンス管理開始" Command="{Binding StartManagementCommand}"/>
                    </Grid>

                    <ListBox Grid.Row="1" ItemsSource="{Binding WorldInstanceManager.Instances}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <Run Text="{Binding InstanceId}"/>
                                    <Run Text="{Binding UserNum, StringFormat={}{0}人}"/>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StatusBar Grid.Row="2">
                        <Button Content="" Width="100" Height="10"
                                Visibility="{Binding WorldInstanceManager.IsManaging, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StatusBar>
                </Grid>
            </TabItem>-->

            <!--Inviteタブ-->
            <TabItem Header="Invite">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <GroupBox Grid.Row="0" Header="ワールド・インスタンス設定" Padding="0 5 0 0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="25"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="ワールドID" VerticalAlignment="Center"/>
                            <Grid Grid.Row="1" Height="25">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <TextBox Grid.Column="0" Text="{Binding WorldId, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center" ToolTip="ワールドID"/>
                                <TextBlock Grid.Column="0" Text="例：wrld_ba913a96-fac4-4048-a062-9aa5db092812" VerticalAlignment="Center"
                                   Margin="5 0 0 0" Foreground="Gray" IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Hidden"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DataContext.WorldId, RelativeSource={RelativeSource AncestorType=Window}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DataContext.WorldId, RelativeSource={RelativeSource AncestorType=Window}}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <ComboBox Grid.Column="1" SelectedValue="{Binding InstanceDisclosureRange}" ItemsSource="{Binding DisclosureRanges}"
                                          VerticalContentAlignment="Center"/>
                                <Button Grid.Column="2" Content="インスタンスを作成" Command="{Binding CreateWorldInstanceCommand}"/>
                            </Grid>

                            <TextBlock Grid.Row="2" Text="インスタンスID" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Text="{Binding InstanceId, UpdateSourceTrigger=PropertyChanged}" VerticalContentAlignment="Center"
                                     ToolTipService.ShowDuration="30000" Height="25">
                                <TextBox.ToolTip>
                                    <TextBlock>
                                <Run Text="インスタンスID"/>
                                <LineBreak/>
                                <LineBreak/>
                                <Run Text="「インスタンスを作成」ボタンを押すとインスタンスが作成され自動入力されます．"/>
                                <LineBreak/>
                                <Run Text="すでに作成したインスタンスを用いる場合は，Webページからインスタンスを作成した際のURL"/>
                                <LineBreak/>
                                <Run Text="（例：https://vrchat.com/home/launch?worldId=wrld_ba913a96-fac4-4048-a062-9aa5db092812&amp;instanceId=00000~region(jp)）"/>
                                <LineBreak/>
                                <Run Text="または，ワールドID:インスタンスID（例：wrld_ba913a96-fac4-4048-a062-9aa5db092812:00000~region(jp)）の形式で入力してください．"/>
                                    </TextBlock>
                                </TextBox.ToolTip>
                            </TextBox>
                        </Grid>
                    </GroupBox>

                    <GroupBox Grid.Row="1" Header="送信先設定" Padding="0 5 0 0">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="16"/>
                                <RowDefinition Height="25"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="25"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="ユーザーリストファイル" VerticalAlignment="Center"/>

                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0" Text="{Binding UserListFilePath}" IsReadOnly="True" VerticalContentAlignment="Center"/>
                                <Button Grid.Column="1" Content="ファイルを選択" Command="{Binding SelectUserListFileCommand}"/>
                            </Grid>

                            <DataGrid Grid.Row="2" ItemsSource="{Binding Users}" AutoGenerateColumns="False" CanUserResizeRows="False"
                                      CanUserAddRows="False" IsReadOnly="True" VirtualizingPanel.ScrollUnit="Pixel">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="ユーザー名" Binding="{Binding Name}" MinWidth="60"/>
                                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="245" CanUserResize="False"/>

                                    <!--Invite送信済みか-->
                                    <DataGridTemplateColumn SortMemberPath="HasInvited" MinWidth="40">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent">
                                                    <Path Data="{StaticResource InviteIcon}" Fill="{StaticResource LightBlueColorBrush}"
                                                      VerticalAlignment="Center" HorizontalAlignment="Center" Stretch="Uniform" Margin="2">
                                                        <Path.Style>
                                                            <Style TargetType="Path">
                                                                <Setter Property="Visibility" Value="Hidden"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding HasInvited}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>

                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="ToolTip" Value="このユーザーにはInviteを送信していません．"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding HasInvited}" Value="True">
                                                                    <Setter Property="ToolTip" Value="このユーザーはInvite済みです．"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!--ワールドにいるか-->
                                    <DataGridTemplateColumn SortMemberPath="IsInWorld" MinWidth="40">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent">
                                                    <Path Data="{StaticResource InWorldIcon}" VerticalAlignment="Center" HorizontalAlignment="Center" Stretch="Uniform" Margin="2">
                                                        <Path.Style>
                                                            <Style TargetType="Path">
                                                                <Setter Property="Fill" Value="{StaticResource GrayColorBrush}"/>
                                                                <Style.Triggers>
                                                                    <MultiDataTrigger>
                                                                        <MultiDataTrigger.Conditions>
                                                                            <Condition Binding="{Binding IsInWorld}" Value="False"/>
                                                                            <Condition Binding="{Binding IsOnline}" Value="True"/>
                                                                        </MultiDataTrigger.Conditions>
                                                                        <Setter Property="Fill" Value="{StaticResource OrangeColorBrush}"/>
                                                                    </MultiDataTrigger>

                                                                    <DataTrigger Binding="{Binding IsInWorld}" Value="True">
                                                                        <Setter Property="Fill" Value="{StaticResource GreenColorBrush}"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>

                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="ToolTip" Value="このユーザーはオフラインです．"/>
                                                            <Style.Triggers>
                                                                <MultiDataTrigger>
                                                                    <MultiDataTrigger.Conditions>
                                                                        <Condition Binding="{Binding IsInWorld}" Value="False"/>
                                                                        <Condition Binding="{Binding IsOnline}" Value="True"/>
                                                                    </MultiDataTrigger.Conditions>
                                                                    <Setter Property="ToolTip" Value="このユーザーはInvite先のインスタンスにいません．"/>
                                                                </MultiDataTrigger>

                                                                <DataTrigger Binding="{Binding IsInWorld}" Value="True">
                                                                    <Setter Property="ToolTip" Value="このユーザーはInvite先のインスタンスにいます．"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>

                                    <!--滞在インスタンスチェックが予定されているか-->
                                    <DataGridTemplateColumn SortMemberPath="IsLocationCheckScheduled" MinWidth="40">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="Transparent">
                                                    <Path Data="{StaticResource ClockIcon}" Fill="{StaticResource FrenchBeigeColorBrush}"
                                                          VerticalAlignment="Center" HorizontalAlignment="Center" Stretch="Uniform" Margin="2">
                                                        <Path.Style>
                                                            <Style TargetType="Path">
                                                                <Setter Property="Visibility" Value="Hidden"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsLocationCheckScheduled}" Value="True">
                                                                        <Setter Property="Visibility" Value="Visible"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>

                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Setter Property="ToolTip" Value="{x:Null}"/>
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsLocationCheckScheduled}" Value="True">
                                                                    <Setter Property="ToolTip" Value="Joinを待っています…"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>

                                <!--セルの垂直中央寄せ-->
                                <DataGrid.Resources>
                                    <Style TargetType="DataGridCell">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type DataGridCell}">
                                                    <Grid Background="{TemplateBinding Background}">
                                                        <ContentPresenter VerticalAlignment="Center"/>
                                                    </Grid>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </DataGrid.Resources>
                            </DataGrid>

                            <!--Invite実行ボタン-->
                            <Button Grid.Row="3" Command="{Binding InviteCommand}">
                                <Grid>
                                    <Viewbox Stretch="Fill">
                                        <ProgressBar VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" Value="{Binding InviteProgress}"
                                                     Visibility="{Binding IsInviting, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    </Viewbox>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Path Data="{StaticResource InviteIcon}" Fill="{StaticResource DefaultIconColorBrush}" Stretch="Uniform" Margin="4"/>
                                        <TextBlock VerticalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Inviteを送信"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsInviting}" Value="True">
                                                            <Setter Property="Text" Value="{Binding InviteProgress, StringFormat=Invite送信中：{0}%}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Grid>
                            </Button>
                        </Grid>
                    </GroupBox>
                </Grid>
            </TabItem>
        </TabControl>

        <!--ログ-->
        <Grid Grid.Row="3" Margin="5 0 5 5">
            <Grid.RowDefinitions>
                <RowDefinition Height="16"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="実行ログ" VerticalAlignment="Center"/>
            <ListBox Grid.Row="1" ItemsSource="{Binding Logs}" VirtualizingPanel.ScrollUnit="Pixel">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBlock Text="{Binding Time, StringFormat=HH:mm:ss}" Width="60" DockPanel.Dock="Left"/>
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" DockPanel.Dock="Right"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Button Grid.Row="2" Content="ログクリア" Command="{Binding ClaerLogCommand}"/>
        </Grid>

        <!--<GridSplitter Grid.Row="3" Height="5" HorizontalAlignment="Stretch" VerticalAlignment="Top"/>
        <Path Grid.Row="3" Data="{StaticResource SplitterMark}" Fill="Gray" VerticalAlignment="Top" HorizontalAlignment="Center"/>-->

        <!--ステータスバー-->
        <StatusBar Grid.Row="4">
            <ProgressBar Width="100" Height="10" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
            <ProgressBar Width="100" Height="10" Value="{Binding InviteProgress}" Visibility="{Binding IsInviting, Converter={StaticResource BoolToVisibilityConverter}}">
                <ProgressBar.ToolTip>
                    <TextBlock Text="{Binding InviteProgress, StringFormat=Invite送信中：{0}%}"/>
                </ProgressBar.ToolTip>
            </ProgressBar>
        </StatusBar>
    </Grid>
</Window>